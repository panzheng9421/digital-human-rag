# Maven 构建产物
target/
*.class
*.jar
*.war

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.project
.classpath

# 本地敏感配置：含真实 API key 的 application.yml 不进仓库
# 克隆仓库后请复制 application.example.yml -> application.yml 并填入你的 key
src/main/resources/application.yml

# 操作系统
.DS_Store
Thumbs.db
*.log

# 误放入的截图/临时图片
*.png
ScreenShot_*

# 本地参考图素材库（250MB+ 二进制，不进仓库；代码用 img.triview.cn 的 URL）
# 仅保留 reference/ 下的文档（.md/.txt）作为接入参考
reference/*.png
reference/*.jpeg
reference/*.jpg
reference/*.PNG
reference/*.JPEG
reference/*.JPG
